import React from 'react';
import styles from './styles.css';


const Searching = () => {
  return (
    <p className={`${styles.loadingSign}`}>Loading our Banks!</p>
  );
};

export default Searching;
